# Copy to .env before starting: cp .env.example .env
# These are local development defaults, not real secrets.

# Keycloak admin (bootstrap)
KC_ADMIN=admin
KC_ADMIN_PASSWORD=admin

# Service account the agent uses (client credentials flow)
MCP_CLIENT_ID=mcp-agent
MCP_AGENT_SECRET=mcp-agent-secret
