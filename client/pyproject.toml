[project]
name = "client"
version = "0.1.0"
description = "Local development client for sending queries to the MCP server"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.4.2",
    "httpx",
    "python-dotenv",
]
